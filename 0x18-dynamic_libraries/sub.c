#include <stdlib.h>
#include <stdio.h>

/**
 * sub - will subtract two integers
 * @a: the first int
 * @b: second int
 * Return: substract the final result
 */
int sub(int a, int b)
{
	return (a - b);
}
