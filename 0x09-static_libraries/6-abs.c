#include "main.h"

/**
 * _abs - prints a function that computes the absolute value of an integer
 * @n: function parameter
 * Return: 0
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-1 * n);
}
